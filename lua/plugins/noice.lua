return {
  {
    "folke/noice.nvim",
    opts = {
      cmdline = {
        -- view = "cmdline", -- Disables the popup and uses the classic bottom command line
        enabled = false, -- Disables Noice's cmdline entirely, falling back to Neovim native
      },
    },
  },
}
